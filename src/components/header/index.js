import React from 'react';

import './style.scss';

const Header = () => (
    <header className="header">
        <div className="header-logo">
			<i className="slideshow-icon material-icons">slideshow</i>
        </div>
        <h3 className="header-title">React Slider</h3>
    </header>
);

export default Header;
