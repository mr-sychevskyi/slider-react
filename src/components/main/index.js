import React from 'react';

import './style.scss';

import Slider from '../../containers/slider';

const Main = () => (
    <main className="main">
        <div className="slider-wrapper">
            <Slider/>
        </div>
    </main>
);

export default Main;
